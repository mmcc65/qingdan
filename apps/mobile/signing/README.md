# 手机正式签名

`private` 目录保存清单手机正式版的私有签名和密码，不进入 Git，也不能上传到公开仓库。

这组文件决定以后生成的 APK 能否覆盖更新手机上的正式版。丢失后无法继续使用同一签名升级，只能卸载重装。因此应将整个 `private` 目录额外备份到至少一个安全位置。

不要把签名文件或密码截图发送给任何人。

本机已生成 `Qingdan-signing-backup.zip` 便于整体备份。该压缩包与 `private` 目录一样属于私密文件，已被 Git 忽略；应再复制到一个只有本人可以访问的安全位置。
